<%= simple_form_for [@list, @bookmark], html: { class: "w-100"} do |f| %>
  <%= f.association :movie, input_html: { class: 'select2' }  %>
  <%= f.input :comment %>
  <%= f.submit class: 'btn btn-primary' %>
<% end %>
